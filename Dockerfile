# Используем официальное Node-окружение
FROM node:20.16.0

# Рабочая директория внутри контейнера
WORKDIR /app

# Копируем только package.json и lock-файл для установки зависимостей
COPY package*.json ./

# Устанавливаем зависимости
RUN npm install

# Копируем весь проект
COPY . .

# Открываем порт (React по умолчанию на 3000)
EXPOSE 3000

# Стартуем React-приложение
CMD ["npm", "start"]
